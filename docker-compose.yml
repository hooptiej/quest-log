services:
  quest-log:
    build: .
    container_name: quest-log
    restart: unless-stopped
    ports:
      - "4242:4242"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=4242
      - DATA_PATH=/app/data/state.json
